# 2026-07-27

## Task 032: OSHA citations, retrieved rather than recalled

The emails hedge every line with "our understanding, not law", and rightly
so — nothing in lib/requirements had a source anyone could check. This adds
one, without asserting anything from memory.

**How it works.** Candidates live in lib/requirements/citations.ts as
proposals, not claims. `npm run verify:citations` fetches each from the eCFR
API, takes the official section heading and the subpart description from the
ancestry endpoint, and keeps it only if the words in `expect` actually turn
up in what came back. What survives is written to verified-citations.json,
which is committed and is the only thing the application reads — the request
path never touches the network, so a review is deterministic and an eCFR
outage cannot reach a contractor.

The subpart matters more than expected. Two sections are published as plain
"General requirements" — 1910.132 and 1926.1203 — and only the subpart says
one is about protective equipment and the other about confined spaces in
construction. Checking the heading alone would have accepted them without
ever confirming the subject, and displaying it alone would have told the
reader nothing.

**Both failure modes were tested, not assumed.** A nonexistent section
(1910.9999) is refused as not found. A real section with the wrong
expectation (1910.147 against "asbestos") is refused with the retrieved
heading quoted back in the reason. The script exits non-zero on any drop,
because a citation list that quietly shrinks is exactly the sort of thing
that goes unnoticed for months.

Twelve citations verified across eight of the twelve requirements, against
Title 29 as current to 2026-07-16.

**What this deliberately does NOT do**, and it is the opposite of what I
expected going in. It does not move anything off `verified: false`. That flag
means a person has checked ISNetworld's or Avetta's own published
requirements — a completely different claim from "an OSHA standard exists on
this subject". lib/requirements warns that citing a regulation for a
contractual requirement is the most damaging mistake that file can make, and
flipping `source` to "osha" because a standard exists would have been exactly
that mistake. So source stays "platform", verified stays false, and the
citation sits alongside under "OSHA standards on this subject" — a reference
to take to a safety adviser, not a statement that they must hold a document.
Whether 1910 or 1926 applies to a given contractor turns on their work and
their site, and this software cannot determine that.

**One validator rule was removed on purpose.** Citations were restricted to
items with source "osha". That conflated two things: who is asking for a
document, and which standard covers the subject. Hazard communication is
both — ISN asks for the programme and 1910.1200 exists — and the rule forced
a choice between dropping a real reference and restating a contractual ask as
law. `supportsClaim` is still required. The reasoning is recorded inline so
nobody restores it as a tidy-up.

**Four requirements carry no citation, and that is the deliverable rather
than a gap:** written safety programme, drug and alcohol, training records,
insurance certificate. No single OSHA standard requires the document a
prequalification platform is asking for in those cases. They stay contractual
and the email keeps saying so.

Build, lint and typecheck clean; schema validation passes on a generated
review. scripts/ is excluded from the app typecheck since it runs through
tsx under different rules.

**Left for human review:** the candidate mappings themselves. Retrieval
proves a section exists and mentions the subject. It cannot prove the mapping
is the right one to put in front of a contractor — that judgement is still
a person's.

## Acting on the citation review

A review came back on the candidate mappings. All twelve section numbers were
confirmed correct. Three things changed as a result, and one point was
checked rather than taken on trust.

**The scope note was the substantive one, and it was verified before acting
on it.** The claim was that 1910.147 excludes construction employment.
Fetching the section text confirms it: "This standard does not cover the
following: (A) Construction and agriculture employment". 1910.146 says the
same. That matters here more than it might elsewhere — a scaffolding or
mechanical subcontractor on a plant turnaround may well be doing construction
work, and showing them a general industry standard with no caveat is close to
misleading.

Rather than hand-write the caveat, the verifier now detects it. It looks for
exclusion language that specifically names construction, which is narrower
than it sounds and deliberately so: 1910.1200 also contains "does not apply
to", but about hazardous waste, and a check for exclusion language alone
would have flagged it wrongly. Exactly the two sections that exclude
construction are flagged; 1910.1200 is not.

**Tightened every expectation** to the reviewer's suggestions - "control of
hazardous energy" plus "lockout/tagout" rather than the looser single term,
"duty to have fall protection", "criteria for personal protective equipment",
and so on. All twelve still verify, which is the useful part: a stricter test
that still passes is worth more than a loose one that always did.

**Documented the matching semantics**, which the review rightly flagged as
unclear. `expect` is a conjunction: every term must appear, matched against
the subpart description and section heading together. Body text is
deliberately not searched - a phrase can turn up anywhere in a long
regulation for reasons unrelated to its subject, and matching on that would
accept almost anything.

**One gap left open on purpose.** 1910.147 excludes construction, and this
file has no construction counterpart for lockout/tagout. Which 1926 standard
fills that role is a domain judgement, not a retrieval - the review itself
said it "may need additional construction-specific electrical, equipment, or
process standards", and guessing at one would be precisely the mistake this
pipeline exists to prevent. It stays empty until a person decides.

Also added tsx as a dev dependency so `npm run verify:citations` resolves.

## Making the refusal machine-verifiable

Second round of review, and the central point was right: an empty citation
array cannot distinguish "not researched" from "researched and deliberately
not mapped", and those need opposite responses from whoever reads it next.

`CITATION_GAPS` now declares the refusal as data — requirement, work context,
status `context-dependent`, stable code `CONTEXT_DEPENDENT_CITATION`, and a
reason. `resolveCitations()` fails closed on it: a declared gap returns no
citation at all rather than the nearest available one, and a request for
construction filters out any standard whose own text excludes construction.
Both matter because the tempting refactor is "we have exactly one
lockout/tagout citation, use it" — and that citation says in its own scope
clause that it does not cover construction.

`CONTEXTUAL_CITATION_CANDIDATES` is typed and empty on purpose. 1926.417 is a
real construction lockout provision, but it is about electrical circuits;
offering it as *the* construction answer would be the over-mapping the gap
exists to prevent. The shape lets a narrower verified mapping be added later
without anyone having to declare a universal counterpart to do it.

The gap is now visible in three places rather than one: the verifier,
tasks.json, and the review email under "Where we stop short".

**The project has tests for the first time.** node:test through tsx, no
framework, eight cases and mostly negative — because the failure worth
catching is not "the right citation is missing", it is "a real, relevant,
plausible section reaches a contractor it does not cover", which looks
correct in every review.

Two of them earn their place immediately. One asserts confined-space DOES
still resolve for construction, guarding against the refusal being
over-broad — 1910.146 also excludes construction, but 1926.1203 was verified,
so there is no gap to declare there. The other asserts 1926.417 does not
satisfy generic construction lockout/tagout.

Mutation-checked rather than assumed: removing the gap declaration fails the
test. Worth noting what did NOT fail — the two tests about 1910.147 still
passed, because the retrieved `excludesConstruction` flag blocks it
independently. Two mechanisms guard the same hole, and removing either alone
does not reopen it.

## Docs brought up to date

README described Scope A and a project with no tests. It now says where
things actually are, leads with the diff-not-judgement idea, and documents
`npm test` and `npm run verify:citations`.

CLAUDE.md said the eCFR check "runs on every submission, automatically". It
does not, deliberately, and that is now recorded as a decision rather than
left looking like a bug: fetching per submission would make identical input
produce different output depending on eCFR's uptime, put a third-party outage
between a contractor and their email, and add seconds to a function that has
already died twice on its wall clock. Also added the rules about declaring
refused mappings and keeping retrieved facts separate from mapping decisions,
and put `npm test` in the definition of done.

---

# Task 039 — client sign-in and a dashboard

A client can now read their own file: what they uploaded, and the review we
produced from it. Requested alongside automatic safety-manual generation,
which is deliberately NOT in this branch — see the note at the end.

## How signing in works

No passwords, and no accounts table. Identity here has only ever been the
email address on a submission, and that address is already the thing that
proves the documents are theirs, because it is where the review was sent.
So: type the address, get a link, click it, and you are in for seven days.

Two JWTs, one HMAC secret in `AUTH_JWT_SECRET`, nothing about a session in
the database. The link token lives 15 minutes, the session cookie 7 days as
asked. They are separated by a `purpose` claim that is checked on the way in
and not merely written on the way out — without it the fifteen-minute link
token would verify perfectly well as a seven-day session cookie, since it
carries the same secret and the same issuer, and the short expiry that bounds
a credential sitting in an inbox would silently stop applying. There is a
test for exactly that, in both directions.

The cookie is httpOnly, `sameSite: lax`, path `/`. `lax` and not `strict`
because the link is clicked from a mail client, which makes the navigation
cross-site — under `strict` the browser withholds the cookie on the way back
and the person lands on the dashboard signed out, having just signed in.

Stateless costs revocation, and it is worth being plain about it: signing out
clears the cookie in that browser and nothing else. A copy taken beforehand
works until it expires. The only global revocation is rotating the secret,
which was tested rather than asserted — rotating it and restarting turned a
live session into a redirect to sign-in.

The link is deliberately not single-use. Making it so needs a row per link,
which is the store this was built to avoid, and it breaks the common case
rather than an attack: corporate mail scanners follow every link in an
incoming email and would burn it before its owner clicked.

## Not confirming who has an account

Requesting a link gives the same answer whether or not the address is known
— same page, same wording. The form is public and the addresses behind it
belong to small contractors, so an honest "no submissions for that address"
turns it into an oracle that confirms, one address at a time, who has sent us
their safety paperwork.

That principle produced a bug and its fix in the same sitting. The first
version reported a failed send, which reads as harmless until you notice a
send only fails for an address that HAS submissions — a stranger's request
never reaches the send. With SMTP broken, that is a clean enumeration oracle
across every address someone cares to try. Now only address-independent
faults are shown (`AUTH_JWT_SECRET` missing, SMTP not configured at all);
an individual send failing is logged and the page says the same thing it
always says, naming the support address for that case.

Rate limited on two keys for two different reasons: by address, so the form
cannot be used to mail-bomb one contractor, and by caller, so a script cannot
walk a list of addresses. Both fail closed into the same confirmation.

## Reading only your own file

Every query in `lib/dashboard.ts` takes the email as its first argument and
filters on it in the query — never fetch-by-id-then-compare. A missed filter
returns nothing; a missed comparison returns someone else's safety paperwork.
"Not yours" and "does not exist" are the same 404, so a guessed uuid reveals
nothing.

Downloads go through `/api/documents/[id]`, which checks the session, confirms
the document is theirs, mints a signed URL good for five minutes and
redirects. The bucket stays private, the bytes go browser-to-Supabase rather
than through a function, and there is no address on the page that could be
copied out and used later or by anyone else. Document ids are uuids but they
are not secrets — they are printed into the markup that links here.

## The review on screen

`components/review-panel.tsx` renders the same content as the review email
under the same rules. Low-confidence items go through a separate component
that has no access to the status labels at all, so "renders as a question,
never as an assertion" is structural rather than a convention someone has to
remember. Citations print only when the stored citation says it supports the
claim, and are labelled as standards on the subject rather than as this
contractor's duty. The three sources stay apart. Files that could not be read
are named in the open, because silence would imply they were reviewed and
fine.

Ten render tests cover those rules against real markup. They needed their own
script: `react-dom` refuses to render DOM under the `react-server` condition
the lib tests use, so `npm test` is now `test:lib` plus `test:ui`. 27 tests
pass overall.

## A real bug the browser found

The verify route originally redirected to `SITE_URL`. The cookie is set on
whichever host the request arrived at, and certloop.net and www.certloop.net
are separate cookie jars — with the apex/www split recorded in DEPLOY.md,
that means signing in successfully and arriving signed out. It now resolves
against the incoming request's own origin. Same class of bug as the canonical
URL finding from task 005, with a worse symptom.

## What is verified and what is not

Verified locally: the guard redirects a signed-out visitor off /dashboard;
`/api/documents/<id>` answers 401 unauthenticated; a garbage token lands on
"expired or already used" and stays on the right host; a valid link token
sets a session that the httpOnly cookie hides from scripts; rotating the
secret revokes it; 27 tests pass; build and lint clean.

NOT verified locally: anything that reads the database. There are no Supabase
credentials in this environment — `.env.local` did not exist before this task
— so the dashboard list, the submission page and a real download have never
run against real rows. The failure observed is exactly and only the missing
credential. Same position as tasks 025 and 026, which were confirmed in
production; this needs the same. HTML email rendering in a real client is
still unverified from task 028, and the sign-in email is plain text only, so
that risk does not extend to it.

## Safety manual generation: deliberately not here

The other half of the request was to read the documents and write the safety
manual automatically. It is not in this branch, by decision rather than by
omission.

`lib/requirements/` holds recognition phrases and a one-line action per
requirement. There is no manual prose in this project to assemble one from,
and CLAUDE.md forbids a language model without an explicit reversal. So the
content would have to come from somewhere, and the options are: a human-
authored template library (deterministic, stays inside the rules, needs
someone to write the source prose), a structural skeleton with no regulatory
prose at all, or reversing the no-LLM decision — which would give up
reproducibility and citability on the one document a contractor hands their
hiring client as their actual safety program. That is the highest-stakes
artifact this product could produce and it is not a choice to make in
passing. Deferred to its own task with the decision left open.

---

# Task 041 — an HTML half for the sign-in email

The sign-in email was plain text; it now uses the same shell as the review,
confirmation and explainer emails. Dark masthead with the tape-rule mark, tick
rule, verdigris button, the site palette by hex, presentation tables and inline
styles throughout.

## Reversing my own reasoning, and why

Task 039 made this email plain text deliberately, with a rationale attached: a
bare URL is the hardest thing for a corporate gateway to mangle, and a scanner
that follows every link would burn the token before its owner clicked. That
was half right, and the half that was wrong is the interesting part. A gateway
that rewrites links rewrites both parts of a multipart message, so plain text
buys nothing there. And the scanner argument does not apply to this token at
all, because it is deliberately not single-use — a scanner opening it costs
nothing.

What plain text genuinely bought was not resembling the phishing mail this
structurally is. The HTML answers that better than plain text did rather than
worse, but only because it was designed to: the destination is printed in
full, in monospace, in a bordered block under the button. Someone who cannot
tell a real sign-in mail from a fake one has no way to inspect a button, and
"hover to check where it goes" is advice that does not survive a phone.

## Three ways it differs from the other emails

No reply-and-we'll-help call to action. Every other email here ends by
inviting a conversation. This one ends by telling someone who did not ask for
it that they can ignore it safely, and a second thing to do would bury the
only line that matters to the person being phished.

No summary of the submission. This is the one message that gets sent to an
address on somebody else's say-so, so it must not confirm anything about what
sits behind that address. There is a test asserting the words "submission" and
"gap check" do not appear.

The URL twice, on purpose — button target and readable text — which is also
the fallback for clients that strip the button.

## Tests

Six, in a new `lib/email-html.test.mts`. The one that earns its place asserts
the href carries the token byte for byte: a token that survives escaping in
the visible copy but not in the attribute produces an email that looks perfect
and signs nobody in. Others cover the stated expiry, the didn't-ask-for-this
block, and that nothing loads from the network — no `<img>`, no
`background-image`, no `<link>`. Remote images are blocked by default in
Outlook and for many Gmail users, so branding that depends on one renders as a
grey box, and an image in automated mail from a young domain is also what a
tracking pixel looks like to a spam filter.

33 tests pass. Rendered and checked in a browser. Still not checked in a real
mail client — the same open risk task 028 left behind, unchanged by this.

---

# Task 042 — the navbar knows whether you're signed in

The "Sign in" link now becomes "Sign out" when there is a session.

## The problem this had to route around

The header is a client component (it needs usePathname for the current-page
underline) and the session cookie is httpOnly, so the header cannot read it.
The obvious fix — read the session in the root layout and pass a prop down —
has a cost that is easy to miss: the root layout wraps every page, so touching
cookies there makes all of them dynamic. Five of the six are statically
prerendered marketing pages whose entire job is to be indexed and fast. Paying
for that with a word in the corner of the chrome is a bad trade.

So: a second cookie, `certloop_signed_in`, set beside the session, not
httpOnly, carrying no authority at all. Nothing server-side reads it. Every
access decision still goes through currentClient() and the signed session
cookie. Forging it in a console changes a word in the header and gets you
nothing else — which is exactly the amount of power it should have.

Verified the point held: after the change, `/`, `/about`, `/faq`, `/contact`
and `/privacy` are all still ○ static in the build output.

## Two things the linter was right about

First attempt read the cookie in a useEffect and set state. ESLint's
`react-hooks/set-state-in-effect` rejected it, correctly — the cookie jar is
an external store, not React state, and mirroring it into state from an effect
is the cascading render `useSyncExternalStore` exists to replace. Rewritten
onto that, which also handles the SSR half properly: `getServerSnapshot`
returns false, so prerendered HTML and the first client render agree by
construction rather than by luck. No hydration warning in the console.

Sign out is a form posting to the existing server action, not a link. A GET
that mutates is something a prefetcher or a link scanner can fire just by
looking at the page.

## Details worth keeping

The hint expires with the session — same maxAge — so the header stops offering
"Sign out" at the moment it stops being true. It can still go stale if
AUTH_JWT_SECRET is rotated, since that invalidates the session without
touching any cookie. That heals on the click: signing out clears both and
lands on the sign-in page, which is where that person needed to go anyway.

The nav's auth item is hidden entirely on /dashboard, where the dashboard's
own bar already shows the account email and a sign-out rendered on the server
from the real session. Two sign-out buttons on one page is clutter, and the
one that survives should be the one that cannot be wrong.

## Verified

Signed out, nav reads "Sign in". With the hint set, it reads "Sign out" and is
a submit button inside a form. Clicking it round-tripped properly: both
cookies cleared, redirected to /sign-in?signedout=1, the signed-out banner
rendered, and the nav flipped back to "Sign in". No console errors, no
hydration mismatch. At 375px the row wraps as it did before with no horizontal
overflow. Build, lint and 33 tests clean.

---

# Task 043 — loading states

Every control that waits on the network now says so. One spinner component,
one submit button that knows its own form's status, a route-level fallback for
the two dynamic dashboard pages, and a pending state on the document download.

## What was already there and what wasn't

The intake form and the contact form already tracked pending and swapped their
label ("Saving…", "Sending…") — they just had no spinner, and the trailing
icon vanished while busy, leaving the button looking emptier rather than
busier. The spinner now takes the icon's place, so the button's shape does not
change when it starts working.

Everything added since — sign-in, both sign-out buttons, the internal gate —
had no pending state at all. The sign-in button is the one that most needed
it: it sends an email, so the wait is a round trip plus an SMTP handshake, and
the honest response to a dead-looking button is to press it again. Three
presses is the per-address rate limit.

## Why a separate SubmitButton component

`useFormStatus` reads the nearest enclosing form and returns nothing useful
when called from the component that renders that form. That constraint turns
out to be a feature: because the hook lives in a child, a plain server-rendered
page can get a pending state by dropping the button in, without the page
becoming a client component. That is how /sign-in, the dashboard layout and
the internal page all got one while staying server components.

## Details that took a decision rather than a default

The spinner is four border declarations rather than an SVG, inheriting
`currentColor`, so the same component works on the verdigris button and on a
plain text link with no variant for either.

`motion-reduce:animate-none` is on it. A continuously spinning element is one
of the things prefers-reduced-motion exists for, and it can genuinely provoke
symptoms in people with vestibular disorders. Confirmed the rule is actually
generated and not just typed: the compiled stylesheet contains
`@media (prefers-reduced-motion: reduce) { .motion-reduce\:animate-none {
animation: none } }`. Stopped, it still reads as a marker beside the label.

The spinner is aria-hidden and every pending state changes the button's text.
The label is what a screen reader announces; a spinner announcing itself as
well would say the same thing twice. That is why `pendingLabel` is a required
prop rather than an optional one.

The download link's pending state is time-limited rather than cleared on
completion, which looks lazy and is not. The navigation ends in a file
download, so the page is never unloaded and no event ever reports that the
file arrived — nothing in the browser reliably says "the download started".
Six seconds covers the wait and leaves a failed click clickable again, which
beats a spinner turning forever over a file that downloaded fine.

For the dashboard pages the answer was a route-level `loading.tsx` rather than
a spinner on the link, which is what Next's own guidance prefers for a dynamic
route: the shell appears immediately and the browser is on the new page while
the data is still being fetched, instead of sitting on the old page. One file
covers both routes, since a nested route with no fallback uses the nearest one
above it.

## Verified in the browser

Not by reading the code — a MutationObserver recorded the transient states,
because all three of these complete too fast to catch by polling.

  sign-in:   "Email me a link" -> "Sending your link…" disabled, spinner
  intake:    "Continue"        -> "Saving…"            disabled, spinner
  sign out:  "Sign out"        -> "Signing out…"       disabled, spinner

The spinner's computed style confirms it: animation `spin`, 1s, transparent
top border, the rest currentColor. No console errors. Build, lint and 33 tests
clean.

Not verified: the dashboard `loading.tsx` and the download pending state have
not been seen against real data, for the same reason as everything else on
those pages — no Supabase credentials in this environment.

---

# Task 044 — a Dashboard link in the nav

Signed in and away from the dashboard, there was no way back to it from the
chrome. The nav offered "Sign out" and nothing else, so someone who wandered
to the FAQ mid-prequalification had to know the URL or go round through
sign-in — which would have bounced them straight to the dashboard anyway, a
confusing way to discover you were never signed out.

Signed in, off the dashboard, the nav is now: About, FAQ, Contact | Dashboard,
Sign out. On /dashboard both are hidden, unchanged from task 042 — that page's
own bar carries the account email and a sign-out rendered from the real
session rather than from the hint.

`prefetch={false}` on it. /dashboard is dynamic and behind a session, so
prefetching would run a JWT verification and a Supabase query for every
signed-in visitor who merely hovered the nav on a marketing page.

## The regression this introduced, which is the part worth recording

Adding a fifth item broke the header at phone width, and only measurement
caught it. The <ul> had no flex-wrap: four items fit a 375px screen by luck,
five did not, and the list ran 29px past the nav's own padding with "Sign out"
pinned against the screen edge. The page reported no horizontal overflow, so
nothing looked wrong from the usual checks — the ul had simply grown wider
than its padded box while the document stayed the same width.

Measured before: ul right edge at 380 against a content edge of 351. After
adding flex-wrap with justify-end: right edge 351, exactly flush, and "Sign
out" wraps to a second right-aligned row. Desktop stays one row at 73px tall.

Worth remembering that "no horizontal scrollbar" is not the same as "fits".

## Verified

Signed out: About, FAQ, Contact, Sign in. Signed in on /faq: Dashboard and
Sign out present, divider on the Dashboard item, five list items. No console
errors, build, lint and 33 tests clean.

Not seen on screen: the nav as it renders on /dashboard itself, since this
environment has no Supabase credentials and /dashboard redirects to sign-in
without a real session. The branch is a one-line pathname check unchanged
since 042.

---

# Task 045 — the navbar hint stops being able to lie

Raised as "SSR is not aware of the user's session". Half of that turned out to
be wrong and half of it turned out to be a real bug, so both are worth writing
down.

## What was already true

SSR is session-aware everywhere it gates anything. The dashboard layout, both
dashboard pages, the document download route and the sign-in page all call
currentClient() on the server and act on it. Proven rather than asserted: with
only the session cookie and no hint, /sign-in answers 307 to /dashboard — the
server verified the JWT and knew exactly who was asking.

## What was actually broken

The navbar, and not merely as the first-paint flash I had already documented.
The hint cookie was written in one place, openClientSession, so it could only
ever exist for a session created after the hint itself existed. Every session
minted before task 042 shipped — which is every session that had been created
on production at that point — had a valid seven-day cookie and no hint. Those
people saw "Sign in" permanently, and clicking it dropped them on the
dashboard, because the server recognised them all along.

That is the confusing state I described in a code comment in task 044 as the
reason for adding a Dashboard link. I wrote it as hypothetical. It was live.

Reproduced before fixing: `/about` served HTML saying "Sign in" while `/sign-in`
simultaneously 307'd to the dashboard, from the same cookie.

## The fix, and the option that was rejected

src/proxy.ts (Next 16's renamed middleware) derives the hint from the session
on every request. The session cookie is the single source of truth and the
hint is a one-bit cache of it, refreshed rather than remembered.

It verifies the token rather than checking the cookie is present. Presence
would pass a tampered token, an expired one, or one signed under a rotated
secret — all cases where the header must stop claiming a session.

It returns without touching the response when the hint already agrees, which
is the overwhelmingly common case. That is not a micro-optimisation: a
Set-Cookie header on a statically cached page is how a CDN decides the
response is no longer shareable, and five pages are prerendered.

The alternative considered and put to the human was reading the session in the
root layout, which would make the SSR HTML itself correct and remove the flash
entirely — at the cost of every page becoming dynamic, including the five
static marketing pages. Rejected in favour of keeping them static. The
remaining cost is real and unavoidable: cached HTML cannot know who is asking,
so a signed-in visitor still sees "Sign in" for one paint before the client
corrects it. Cache Components (Next 16 PPR) would remove even that, but it
inverts the project's caching defaults and is not worth that risk for a word
in the chrome.

## Verified

Four cases by response header, which is where the behaviour actually lives:

  valid session, no hint    -> set-cookie mints the hint
  valid session, hint set   -> no set-cookie at all
  hint, no session          -> set-cookie clears it
  hint, unverifiable token  -> set-cookie clears it

Then end to end in the browser from the exact broken state — session cookie
only, no hint. The hint was minted on that request and the nav read
"Dashboard / Sign out" as soon as it hydrated, with no second navigation.

Six proxy tests, 39 total. Build, lint clean, and the five marketing pages are
still ○ static in the build output, which was the whole point of choosing this
over the root layout.

---

# Scope C planning, and task 046 — copy truth and trust

## The plan

`docs/scope-c-plan.md` sets out nine stages taking the product from a free
document review to a workspace: copy truth, four entry points, a reusable
company profile, the dashboard as a workspace, review hierarchy, plan states
and service requests, then document generation, consultants and maintenance.
Tasks 046-053 track them. Nothing in it reverses the no-LLM decision or the
regulatory output rules.

## What was actually wrong

Reading the site against the code, the public copy described a process this
repo stopped running in task 027. The homepage said a person on our side did
the comparison by hand. About said "there is no dashboard behind this page"
— there has been one since task 039 — and that the comparison was manual. The
FAQ promised an email written by a person within a few business days.

All of it false since Scope B shipped. That is worse than a tone problem: a
visitor was told to expect a handwritten reply in a few days and got an
automated one in a minute. Fixed first, and before any later stage writes
copy that would inherit the same voice.

## What changed

How-it-works step 2 now describes what actually happens — documents read for
the programs and records these platforms ask for, unreadable files listed as
unread. About gets a new "How the review is produced" section covering the
deterministic pipeline, the no-language-model decision and the eCFR citation
rule, replacing the paragraph claiming there was no dashboard. The FAQ's
timing answer says a minute or two, and a new "Is it AI?" answer states the
no-LLM choice and what it buys, since that is now a question a contractor
will reasonably ask.

The defensive self-narration went with it. "A small and genuinely early
operation — early enough that a person still reads every submission by hand.
There is no team page here because inventing one would be the first dishonest
thing on the site" became a straight answer about independence and an address
to ask at. Same on the contact page's "CertLoop is small and early", and the
FAQ intro that advertised its own honesty.

Every limitation is still on the page: not affiliated, not a consultant, not a
compliance determination, not legal advice. They read as facts now rather than
as reassurance about our character.

## Note

`npm test` failed on arrival with two auth tests erroring on a missing `jose`
module. Not a code fault — `node_modules` was stale after the pull and the
dependency was already declared. `npm install` fixed it. 39 lib tests and 10
UI tests pass, build and lint clean, and the five marketing pages are still
static in the build output.

---

# Task 047 — four entry points into one workspace

## What changed

Three new doors alongside the gap check: `/rejection`, `/setup`, `/documents`.
They render one component with different framing and all post to the same
intake, so a rejection is a submission with rejection context on it rather
than a parallel system. Migration 0005 adds `entry_reason` and
`rejection_notes` to `submissions`; existing rows default to `gap_check`,
which is accurately what they were.

The home page now leads with "What brought you here?" above how-it-works,
because "is this for my problem?" comes before "how does it work". The
headings are the visitor's sentence — someone holding a rejection notice
scans for the word rejected, not for "gap analysis".

## The rejection reader

The door would have been a lie without this, so `lib/analysis/rejection.ts`
reads the pasted notes for real. It reuses each requirement's existing
recognition phrases against the pasted text, with the same word-boundary rule
the document matcher uses, and names the subjects it finds.

The line it holds: "your notes mention lockout/tagout" is a fact about a
paragraph the contractor gave us; "the reviewer requires lockout/tagout" is a
claim about a portal we cannot see. The second is the natural sentence to
write, which is why this is a separate module rather than a few lines in the
matcher — a rejection is the moment someone is most inclined to believe what
they are told, because somebody with authority has just told them they are
wrong.

Matched subjects are sorted to the top of the item list and named in the first
line of the summary, attributed to the notes in the same sentence. Nothing
else about an item changes for having come through that door: no item is
added, removed or re-scored, and the subject can still come back `present` —
which is a useful answer, meaning the file covers it and the reviewer wanted
something else about it.

Two rules are enforced in the validator rather than trusted: a named subject
must carry the phrase it was recognised by, and subjects cannot exist without
notes to have found them in. The rejection block is optional in the schema, so
reviews stored before today still validate on the way back out.

## Verified

15 new tests, 54 in total, build and lint clean. The negative ones are the
point:

  - "photo" does not name a lockout/tagout rejection via "loto"
  - notes matching nothing produce a question, not a nearest guess
  - a paste made on /rejection is dropped if the form is submitted from
    another door, so a reviewer's words cannot attach to a submission that
    is not about a rejection
  - an oversized paste is refused rather than truncated, since the end of a
    reviewer's comment is where the specific request usually is
  - a subject with no phrase, or with no notes behind it, fails validation

In the browser: the paste field renders only on /rejection and is genuinely
absent elsewhere in the DOM rather than hidden, and the four doors are on the
home page. All three new routes are static in the build output and are in the
sitemap above About and FAQ — someone searching "ISNetworld document
rejected" is describing their problem, and that page answers it.

## Not done here

`/documents` records the request and runs the ordinary review; it does not
produce documents. That is task 040, still blocked on reviewed source prose.
The page says so rather than implying a manual arrives.

---

# Task 049 — the dashboard becomes a workspace

## What it was

A list of past submissions, which answers a question nobody arrives with. The
person opening it has two things in mind: what do I do next, and what is
holding me up.

## What it is now

One "Do this next" panel at the top, at full size, with exactly one action in
it — two competing primary actions is the same as none. Then, only where there
is something real to show: what's holding you up, files we couldn't read,
still to confirm, your documents, your requests.

The next action is derived rather than guessed, and `lib/workspace.ts` orders
the branches by what is actually blocking the person rather than by what is
easy to render. An unfinished form comes first, because it produces no review
at all and every other instruction is premature. A rejection with neither the
reviewer's wording nor a file comes next, because a client is already waiting
and we genuinely cannot help until something arrives. Only then the ordinary
"read your review".

Documents are now one library across every request. That is how the person
thinks about it — they sent their safety manual once, and which intake it rode
in on is our bookkeeping, not theirs.

## Two decisions worth recording

`unknown` items are deliberately NOT blockers. An unknown means we had nothing
to go on — no file, or one we could not read — and promoting it to "what's
holding you up" would tell someone a document is missing when what we actually
established is that we never looked at one. They go to the questions instead.

There is no expiry panel, and that is not an omission. Nothing in this product
knows when a certificate expires: we are never told a renewal date and we do
not read one out of a document. An empty "Expiring soon" section reads as
"nothing is expiring", which is a claim we cannot make. It belongs to task
053, once there is a real source for the dates.

## Verified

8 new tests: 52 lib tests and 10 UI tests pass, build and lint clean. The
negative ones again:

  - an unknown item does not appear as a blocker
  - an unfinished form outranks a review with findings in it
  - a rejection that arrived with notes is not chased for them
  - no deadline is reported when none was given; deadline_unknown does not
    become a date on the header

NOT verified against a database. This environment has no Supabase
credentials, so the populated workspace has never rendered against real rows —
signing out redirects correctly and that is all this proves. It needs the same
production confirmation tasks 025, 026 and 039 are still carrying, and
migration 0005 needs running before any of it works at all.

---

# Task 054 — three faults the first real dashboard showed

Production, real rows, one screenshot. The workspace worked, and it was wrong
about three things — none of which the tests could have caught, because all
three were about data no fixture had.

## The button did not work

"Finish the form you started" linked to `/dashboard/<id>`, which shows
documents and a review. A partial submission has neither. The single primary
action on the page led to a page that told them there was nothing there.

Fixed with `/dashboard/[id]/continue`, which resumes the intake at the step
after the last one completed, seeded with what is already stored. Nothing new
on the server: every step already posts its `submission_id` and updates that
row, so resuming was only ever a question of starting the form somewhere other
than a blank step 1. `rowToValues` was already there for echoing a rejected
step back, and does this job unchanged.

Ownership is checked in the query rather than taken from the URL, which
matters more here than on the read-only routes — the id this page hands to the
form is a write capability over that row. A completed submission redirects to
its review instead, so the route cannot be used to resubmit a finished intake.

## "Avetta, Both, ISNetworld"

"Both" is an answer on a form, not a platform. Listed beside the real names it
reads as three platforms and names one of them twice. It now expands to the
two names and de-duplicates. "Not sure" is dropped entirely — it is the
absence of an answer, and printing it in a list of what someone is registered
for states something they told us they did not know.

## "Plant wants you approved by July 24, 2026"

Written on 27 July. The date had gone and the sentence was in the present
tense.

The passed date is deliberately kept rather than filtered out — it is the most
urgent thing on the page, not the least — but it is now flagged, worded in the
past tense and shown in the warning colour.

## Verified

6 new tests, 58 lib and 10 UI passing, build and lint clean. The resume route
is dynamic, as it must be — it reads a cookie.

Still needs a look on production: resuming an actual abandoned intake and
sending it through to a review.

---

# Task 050 — the review says its answer first

## What was wrong

The panel grouped items into three confidence bands: reasonably sure, looks
likely, couldn't establish. That is how the review is produced, not how it is
read. Someone opening it wants "what am I missing" first and had to read every
card in all three bands to work it out. Underneath each card, the basis and the
CFR citations sat at the same weight as the finding itself, so the answer
competed with its own footnotes.

## What it is now

A conclusion box at the top: one line stating how many of the checked document
types were not mentioned, then the summary. Then the standing limitation, once,
under the conclusion rather than above it — it has to be unmissable, but
leading with it meant the first thing a contractor read was a disclaimer rather
than their answer.

Then: what looks missing, what looks covered, worth checking yourself. The
basis, the source label and the standards move into a "Why we say this"
disclosure on each card. Native <details>, so it costs no JavaScript and stays
in the DOM while closed — nothing was dropped, and traceability is intact.

## The rule that had to survive the reshuffle

A low-confidence item is never stated as a fact. Regrouping by status put that
at risk in a specific way: a ticked-but-not-found item is status "present" at
low confidence, and matching on status first would have filed it under "What
looks covered" with a green "Looks present" against it — telling someone a
document is in hand because they ticked a box.

So the confidence filter runs FIRST and anything low goes to `Question`,
whatever its status. `Question` still has no access to STATUS_LABEL, so a
status cannot be printed there even by mistake. Two new tests cover exactly
this, including that a low-confidence item does not inflate the count in the
conclusion.

## Incidental

The FAQ's icon-swap CSS was the only way to do a caret inside a <summary> —
transforms do not apply to descendants of one, which globals.css already
records. Generalised `.faq-icon-*` to `.disclosure-icon-*` and pointed both the
FAQ and the review panel at it rather than writing the same quirk twice.

## Verified

12 UI tests and 58 lib tests pass, build and lint clean.

---

# Task 048 — the company profile

Trade, states, headcount, platform and hiring client were retyped on every
submission, because a submission was the only record this product had.
Migration 0006 adds `companies`, keyed by email — the same identity used
everywhere else, since there is still no accounts table and no password.
`/dashboard/company` edits it, and the dashboard header shows the company name
once there is one.

## field_sources, and why it is a column

Anything this product did not hear directly from the contractor is stored with
where it came from and whether they have confirmed it. Nothing infers anything
today; the seam is here so the first thing that does cannot skip it.

The reason it is structural rather than a convention: the profile sits
underneath a compliance review, and the trade on it decides which requirements
a contractor is measured against. A trade guessed from a company name, stored
unmarked, would silently choose the list they are checked against — and a
company name reveals nothing about how a crew actually works. So an unmarked
value means they typed it, an inferred one renders as a question against the
field, and the dashboard says so where they actually are rather than waiting
for them to visit the profile page.

Confirming keeps the entry with a timestamp rather than deleting it: inferred
then confirmed is a weaker fact than volunteered, and the record should say
which it was.

## Smaller decisions

Only the name is required. This audience fills forms in between jobs, and a
half-filled profile is worth more than one abandoned because it demanded an
EMR. Hiring clients are one textarea, a name per line, because that is what
someone pastes out of an email. Native selects rather than the intake form's
Listbox components — this is a settings screen visited occasionally, not the
funnel.

`company_id` on submissions is nullable and stays nullable. Ownership is still
decided by the email on the submission; that column is convenience, not
identity.

The action writes against the address in the session cookie and never one from
the form. The profile is keyed by email, so trusting a posted address would let
anyone overwrite anyone else's company by typing it into a field.

## Verified

6 new tests, 64 lib and 12 UI passing, build and lint clean. Migration 0006
needs running before the page works at all.
