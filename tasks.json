{
  "tasks": [
    {
      "id": "001",
      "title": "Scaffold Next.js project",
      "status": "done",
      "notes": "Done on branch task/001-scaffold. Next.js 16 App Router + TypeScript + Tailwind v4 + ESLint, src/ layout. Brand strings centralised in src/lib/constants.ts (SITE_NAME). .env.local.example added; .gitignore already covers .env*. Build and lint pass clean. Repo had no commits, so main was created from the existing docs as a base for task branches.",
      "needs_human_review": false
    },
    {
      "id": "002",
      "title": "Build the landing page from scratch",
      "status": "done",
      "notes": "Done on branch task/002-landing-page (branched off task/001, since main has no scaffold yet). Header, hero, how-it-works, gap-check form, footer. Original palette (galvanise/paper/zinc-dust/millscale/slate-wash/verdigris) and type pairing (Bricolage Grotesque over IBM Plex Sans/Mono); signature detail is a tape-measure tick rule in SVG. Icons are lucide-react, no emoji. Copy grounded in business-model.md with TODO-VERIFY comments guarding the unverified consultant figures and the compliance categories. Form renders but is not wired — submitting shows an honest 'not connected yet' notice for task 003 to replace. Build and lint pass clean.",
      "needs_human_review": false
    },
    {
      "id": "003",
      "title": "Set up Supabase lead capture",
      "status": "done",
      "notes": "Done on branch task/003-supabase-lead-capture. The outstanding manual step is complete: the human ran supabase/migrations/0001_leads.sql in the Supabase SQL editor and confirmed a real form submission landed as a row in the leads table, which closes the one gap that was left unproven. Everything else was already done and verified against the live project: server action with server-side validation, allow-list checks on both dropdowns, field-level and form-level errors, values echoed back on failure, pending state, and a real success panel replacing the old 'not wired up yet' placeholder. Verified in the browser: validation rejects whitespace-only and malformed input, the database failure path shows a generic message while the real error goes to the server log only, and the success panel renders.",
      "needs_human_review": false
    },
    {
      "id": "004",
      "title": "SEO essentials",
      "status": "done",
      "notes": "Done on branch task/004-seo (branched off task/002). sitemap.ts and robots.ts via Next.js file conventions, both driven by SITE_URL. JSON-LD Organization schema on the homepage, limited to facts that hold today. Canonical URL plus Open Graph and Twitter metadata. Heading hierarchy and lang verified in the rendered DOM. Alt text audit: no raster images exist yet, and all decorative SVGs are aria-hidden. Page is fully static; trimmed unused IBM Plex weights, which cut roughly 50KB of fonts and one preload. No OG image yet — deferred until the name and domain are settled. Build and lint pass clean.",
      "needs_human_review": false
    },
    {
      "id": "005",
      "title": "Deploy to Vercel",
      "status": "done",
      "notes": "Deployed and verified. The repo is on Vercel, the environment variables are set, certloop.net is attached, and a real form submission on production was confirmed landing in the Supabase leads table — which is the check that actually proves the env vars are right, since the page is static and a green build proves nothing about the form. Live checks: all four pages, the generated OG image, the favicon and the sitemap return 200. Two follow-ups came out of the live check and are written up in DEPLOY.md under 'Post-deploy findings': the apex 308-redirects to www while NEXT_PUBLIC_SITE_URL is the apex, so canonical URLs and the sitemap advertise a hostname that immediately redirects; and the domain is proxied through Cloudflare, whose managed robots.txt prepends a Content-Signal block that disallows a list of AI crawlers. Neither is a code change and neither blocks anything.",
      "needs_human_review": false
    },
    {
      "id": "006",
      "title": "Navbar, smooth scroll, and footer rework",
      "status": "done",
      "notes": "Done on branch task/006-site-expansion. Nav is a flat row of About/FAQ/Contact that stays visible at every width — no hamburger. 'Free gap check' is gone from the nav as asked; it survives as a footer link. Link list lives in src/lib/nav.ts so the header, footer and sitemap share one source. Header and footer moved from page.tsx into the root layout so every page gets the same chrome. Smooth scroll is CSS scroll-behavior on html, wrapped in prefers-reduced-motion so it costs no JS and respects the OS setting. Footer now carries the wordmark, the ISN/Avetta disclaimer, a page list, contact details, a tick rule, and a copyright line with the build year.",
      "needs_human_review": false
    },
    {
      "id": "007",
      "title": "About, FAQ and Contact pages",
      "status": "done",
      "notes": "Done on branch task/006-site-expansion. Three pages, all statically prerendered, each with its own Metadata API entry, canonical, and Open Graph tags, and all three added to the sitemap via NAV_LINKS. Copy is written against business-model.md's trigger event and audience: no founding story, no team bios, and the FAQ says plainly that there is no team page because inventing one would be the first dishonest thing on the site. About covers the situation, what you get today, what we are not, why it is free, and where it goes. FAQ is eleven real questions including the ones where the honest answer is that we are the wrong people to ask; answers are a single array feeding both the page and FAQPage JSON-LD so the two cannot drift. Contact is the form, one email address, what to include, and an explicit list of what we cannot help with. Shared PageIntro/PageSection components keep the rhythm identical to the home page. No specific regulatory claims anywhere — a TODO-VERIFY note in the FAQ source guards against adding any.",
      "needs_human_review": false
    },
    {
      "id": "008",
      "title": "Typography consistency audit",
      "status": "done",
      "notes": "Done on branch task/006-site-expansion. Moved the type scale into globals.css as .type-h1/.type-h2/.type-h3/.type-label/.type-lede/.type-body alongside the existing .tag, and refactored the home page components onto it, so heading sizes are defined once instead of as ad-hoc utility strings per heading. The two large steps are fluid via clamp, replacing the old text-4xl/sm:text-5xl/md:[3.4rem] ladder. Found and merged two near-duplicate label sizes (0.95rem in the hero card, 0.875rem on form labels) into one .type-label step, and consolidated three copies of the primary button style into .btn-primary. Verified in the browser across all four pages: only Bricolage Grotesque, IBM Plex Sans and IBM Plex Mono are computed anywhere, with no system font fallback, and h1 renders identically at 54.4px and h2 at 36px on every page.",
      "needs_human_review": false
    },
    {
      "id": "009",
      "title": "Tagline exploration",
      "status": "done",
      "notes": "Done on branch task/006-site-expansion. Seven options drafted against business-model.md's trigger event, urgency, and the consultant-versus-template contrast; all seven are written up with reasoning in logs/2026-07-25.md. Rather than make one line do two incompatible jobs, the constant is split: SITE_TAGLINE keeps the literal, platform-name-carrying line because it has to work in a search result, and a new SITE_SLOGAN defaults to 'The gap between your file and the gate.' for places with no room to explain, currently the OG card. The other five options sit in a comment directly above the constant in src/lib/constants.ts so swapping is a one-line change while the name is unsettled.",
      "needs_human_review": false
    },
    {
      "id": "010",
      "title": "Dynamic OG image",
      "status": "done",
      "notes": "Done on branch task/006-site-expansion. src/app/opengraph-image.tsx generates the 1200x630 card via next/og, tracking SITE_NAME and SITE_SLOGAN. The tape motif is structural rather than trim: the tick rule runs full width at the same 16px minor / 80px major pitch as the on-page rule, and a verdigris dimension line measures a span against it with its caps landing on major ticks. Satori cannot see next/font and cannot read the .woff2 it emits, so @fontsource/bricolage-grotesque and @fontsource/ibm-plex-mono were added and the .woff files are read off disk — safe because the route builds static. Found and fixed a real bug while verifying: a page declaring its own openGraph object replaces the file-convention image rather than merging, so About, FAQ and Contact were shipping with no card at all; a shared pageMetadata() helper in src/lib/metadata.ts fixes it and removes the triplicated metadata. Home page twitter:card upgraded to summary_large_image. Verified by rendering the PNG and inspecting it, and by checking the meta tags on all four pages.",
      "needs_human_review": false
    },
    {
      "id": "011",
      "title": "Vercel Speed Insights",
      "status": "done",
      "notes": "Done on branch task/006-site-expansion. @vercel/speed-insights installed and <SpeedInsights /> mounted in the root layout per Vercel's setup instructions, with no further configuration. Confirmed in the browser console that it initialises and reports debug mode locally, sending nothing — real data only starts once the site is deployed, which is task 005.",
      "needs_human_review": false
    },
    {
      "id": "012",
      "title": "Collapse the site to a single typeface",
      "status": "done",
      "notes": "Done on branch task/006-site-expansion. The three-family setup (Bricolage Grotesque display over IBM Plex Sans body and IBM Plex Mono labels) is gone; everything is Bricolage Grotesque, differentiated by weight, size and letter-spacing rather than by typeface. --font-display and --font-mono were deleted rather than aliased to the same value, so a stray font-mono or font-display utility fails visibly in review instead of silently falling back to a system stack; all such utilities were removed from the components. The OG image was switched too — it now loads Bricolage at 400 and 700 instead of Bricolage plus Plex Mono, with explicit font weights, since registering two weights under one family made Satori default to 400 and would otherwise have rendered the headline in regular. IBM Plex Sans and Plex Mono dropped entirely: the built home page now references one font file instead of three. Verified in the browser that every text-bearing element on all four pages computes Bricolage Grotesque, with no system fallback anywhere. Note this overrides the CLAUDE.md guidance to pair a display face with a separate body face.",
      "needs_human_review": false
    },
    {
      "id": "013",
      "title": "Vercel Web Analytics",
      "status": "done",
      "notes": "Done on branch task/006-site-expansion. @vercel/analytics installed and <Analytics /> from @vercel/analytics/next mounted in the root layout alongside <SpeedInsights />, per Vercel's setup instructions, with no further configuration. Confirmed present in the rendered HTML. Like Speed Insights it only reports once the site is deployed to Vercel, which is still task 005. Also re-confirmed @vercel/speed-insights is installed at ^2.0.0 and mounted — that was already in place from task 011.",
      "needs_human_review": false
    },
    {
      "id": "014",
      "title": "Point the site at certloop.net",
      "status": "done",
      "notes": "Done on branch task/006-site-expansion, merged to main. certloop.net is registered: SITE_URL falls back to https://certloop.net and CONTACT_EMAIL is info@certloop.net, which is registered and live. NEXT_PUBLIC_SITE_URL is set to https://certloop.net on Vercel. SITE_URL is normalised via toOrigin() rather than used raw, because the value was previously passed straight to new URL() for metadataBase, where a bare hostname throws and fails the build outright; a missing scheme now gets https, trailing slashes and paths are stripped, and an unparseable value falls back instead of breaking the build. Verified against five input forms and with real builds using both the bare and full-scheme values. The domain is now attached and serving. One thing left unresolved by the attach: the apex 308-redirects to www.certloop.net, while NEXT_PUBLIC_SITE_URL is the apex, so every canonical, og:url and sitemap entry names a hostname that immediately redirects to a different one. Fix is one setting either way — make the apex primary in Vercel, or point the variable at www — and is written up in DEPLOY.md. CONTACT_EMAIL is info@certloop.net, registered and live.",
      "needs_human_review": false
    },
    {
      "id": "015",
      "title": "Favicon from the logo mark",
      "status": "done",
      "notes": "Done on branch task/006-site-expansion. The default Next.js favicon.ico is deleted and replaced by src/app/icon.svg, which draws the wordmark's tape graduation. It is a solid verdigris field with the ticks knocked out in paper rather than verdigris marks on transparency, because at 16px a hairline figure on a transparent ground disappears against dark browser chrome; strokes are also heavier than the header wordmark so they survive downscaling. Verified by rasterising it at 16, 32 and 64px and looking at the result — it reads as a tape rule at all three. Confirmed the browser now requests /icon.svg and that /favicon.ico returns 404.",
      "needs_human_review": false
    },
    {
      "id": "016",
      "title": "Email lead submissions to the inbox (SMTP)",
      "status": "done",
      "notes": "Done and confirmed working in production. SMTP credentials were added and deployed, and a real submission delivered the notification. Implemented in src/lib/notify.ts with nodemailer over plain SMTP, using whatever provider hosts the mailbox — no third-party sending service and no API key. Configured with SMTP_HOST, SMTP_PORT, SMTP_USER and SMTP_PASSWORD plus optional LEAD_NOTIFY_FROM and LEAD_NOTIFY_TO; all server-only, none may take a NEXT_PUBLIC_ prefix. Supabase remains the record of truth. Properties: the send happens only after the row is inserted and every failure is caught, so mail can never fail a submission; config is all-or-nothing; and TLS is mandatory, with 465 implicit and every other port required to STARTTLS before authenticating.",
      "needs_human_review": false
    },
    {
      "id": "017",
      "title": "Collapsible FAQ answers",
      "status": "done",
      "notes": "Done. The FAQ is now eleven native <details>/<summary> disclosures instead of a flat <dl>, so opening and closing costs no JavaScript and keyboard support comes for free. All start collapsed, and the answers stay in the DOM whether open or closed — confirmed 490 characters present in the server HTML of a collapsed answer — so the page remains fully indexable and the FAQPage JSON-LD still matches what is rendered. Questions became h3 inside the summary, which improves the heading hierarchy over the previous dt. Getting the open/closed indicator right took three attempts worth recording: Tailwind's group-open:rotate-180 sat in the class list but generated no CSS at all; an explicit CSS rotate rule also failed, and browser testing showed why — a transform on a descendant of <summary> resolves to an identity matrix, proven by the identical element rotating correctly once moved outside the summary. The working approach swaps two icons with display instead of rotating one. Verified in a production build: correct icon in each state, expands and collapses, and only ever one icon visible.",
      "needs_human_review": false
    },
    {
      "id": "018",
      "title": "Confirmation email to the submitter",
      "status": "done",
      "notes": "Done. A submission now sends two emails over the same SMTP transport: the internal copy to info@certloop.net with reply-to set to the contractor, and a receipt to the contractor with reply-to set to info@certloop.net. The confirmation repeats what they submitted so they have a record, and carries the ISN/Avetta disclaimer — it is the one piece of the product that arrives away from the site, where the footer's context is not visible. Copy promises only what the site already promises: a person reads it, it is not instant, one email comes back, no mailing list. The two sends are settled independently rather than awaited in sequence, so a confirmation bouncing on a mistyped address cannot also cost the internal copy, which is the one proving the lead exists. Verified by composing both messages through a nodemailer jsonTransport and reading the envelopes: recipients and reply-to are correct and not swapped, which was the real risk. Also verified against an unreachable server that both sends are attempted, both failures logged separately, and nothing throws. One thing flagged in DEPLOY.md rather than fixed: the form is public and now mails an address the submitter types, which is a small spam-amplification vector; rate limiting was out of scope and is the fix if traffic warrants it.",
      "needs_human_review": false
    },
    {
      "id": "019",
      "title": "Header and footer chrome, sticky nav, and FAQ accordion behaviour",
      "status": "done",
      "notes": "Done on branch task/019-chrome-and-faq-polish. Five changes asked for after reviewing the live site. The footer's 'Email only — we don't run a phone line yet' line is gone; the contact page keeps its own longer version, where it answers a question the reader arrived with. Header and footer now sit on solid millscale instead of being separated from the content by a hairline, bookending the light page rather than introducing two new tones. Three corrections were needed to make text work on that background and they live together in a .surface-dark class in globals.css: a new --color-verdigris-pale (#7fc9b5) because verdigris at full strength measures about 2.5:1 against millscale, a readable .tag colour, and a focus ring that is visible on near-black. Wordmark gained a tone=\"invert\" prop for the same reason. The navbar is sticky with no JavaScript, and html carries scroll-padding-top so in-page anchors do not land behind it — verified that #gap-check lands 47px clear of the 73px header. The FAQ opens one item at a time via the native name attribute on <details>, keeping the page zero-JavaScript as task 017 intended; it needs Chrome 120 / Safari 17.2 / Firefox 130, and older browsers degrade to the previous several-open behaviour rather than breaking. FAQ questions are select-none so dragging to click no longer highlights them, while the answers stay selectable. Verified in the browser: sticky position and dark background on scroll, one chevron per item per state, opening a second item closes the first, summary computes user-select none while answers compute auto, and no 'phone line' text remains. Build and lint pass clean, all ten routes still static.",
      "needs_human_review": false
    },
    {
      "id": "020",
      "title": "Custom 404 page",
      "status": "done",
      "notes": "Done on branch task/019-chrome-and-faq-polish, alongside task 019. Unmatched URLs were falling through to the framework's default not-found page, which ships its own styles and follows the visitor's dark-mode setting — so a mistyped address on a deliberately light-only site landed on a black screen with no site chrome and no way back. src/app/not-found.tsx replaces it: the root not-found file catches every unmatched URL, not only an explicit notFound() call, and renders inside the root layout, so it carries the same header, footer, palette and type as every other page. Copy assumes someone who followed a stale link while working to a deadline — it states that nothing already submitted is affected, then points at the gap check and the three interior pages with a line each on what they cover, keyed off NAV_LINKS so a page added to the nav without a description is a TypeScript error rather than a silent gap. Limitation recorded in the log: Next supports a metadata export only on the experimental global-not-found convention, so the tab title falls back to the layout default; switching would bypass the root layout and lose the shared chrome, which is the whole point, so it stays. noindex is injected automatically for 404 responses. Verified in the browser: real 404 status, one h1, site header and footer present, light background, noindex tag emitted, every link points somewhere real. Build and lint pass clean.",
      "needs_human_review": false
    }
  ],
  "current_task_id": null,
  "blocked_on": {
    "task_id": "005",
    "date": "2026-07-24",
    "action_needed": "Import the repo into Vercel, set NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY and NEXT_PUBLIC_SITE_URL, then submit the live form once and confirm the row lands in Supabase. Full runbook in DEPLOY.md. Do not attach a custom domain yet.",
    "why_a_human": "Connecting the repo means signing in to a Vercel account and authorising its GitHub access. There is no Vercel CLI, API token, or existing session in this environment, and entering account credentials is not something I should do on your behalf.",
    "branches": [
      "task/005-vercel-deploy"
    ],
    "branch_note": "Tasks 001, 002, 004 and 003 are all merged into main and pushed, in that order. task/005-vercel-deploy adds DEPLOY.md and this status update on top of main."
  }
}
